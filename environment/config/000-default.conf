<VirtualHost *:80>
    ServerAdmin webmaster@localhost
    ServerName cruithne.scottsprogram.local
    AccessFileName .htaccess
    DocumentRoot /var/www/cruithne/
    Redirect / https://cruithne.scottsprogram.local/

    SetEnv MAGE_RUN_CODE "base"
    SetEnv MAGE_RUN_TYPE "website"

    <Directory />
            Options FollowSymLinks
            AllowOverride None
    </Directory>
    <Directory /var/www/cruithne >
            Options Indexes FollowSymLinks MultiViews
            AllowOverride All
            Order allow,deny
            allow from all
    </Directory>

    ErrorLog ${APACHE_LOG_DIR}/error.log
    CustomLog ${APACHE_LOG_DIR}/ssl_access.log combined
</VirtualHost>

<VirtualHost *:80>
    ServerAdmin webmaster@localhost
    ServerName cruithne.mygro.local
    AccessFileName .htaccess
    DocumentRoot /var/www/cruithne/

    SetEnv MAGE_RUN_CODE "mygro"
    SetEnv MAGE_RUN_TYPE "website"

    Redirect / https://cruithne.mygro.local/

    <Directory />
            Options FollowSymLinks
            AllowOverride None
    </Directory>
    <Directory /var/www/cruithne >
            Options Indexes FollowSymLinks MultiViews
            AllowOverride All
            Order allow,deny
            allow from all
    </Directory>

    ErrorLog ${APACHE_LOG_DIR}/error.log
    CustomLog ${APACHE_LOG_DIR}/ssl_access.log combined
</VirtualHost>
# vim: syntax=apache ts=4 sw=4 sts=4 sr noet
